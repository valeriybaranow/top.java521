Задача: «Банк и кассиры»
🎯 Цель
Вы реализуете многопоточную модель банка, в которой несколько кассиров одновременно обслуживают клиентов. Кассиры могут вносить деньги в кассу (пополнение) или выдавать деньги из кассы (выдача).

📌 Что делает программа?
Программа моделирует работу 5 кассиров, у каждого из которых есть очередь клиентов с заявками на определённую сумму:

3 кассира занимаются выдачей денег клиентам (если в кассе есть деньги).

2 кассира занимаются пополнением кассы (вносят деньги от клиентов).

Все кассиры работают одновременно (в разных потоках) и используют общую кассу (переменная balance). Доступ к кассе синхронизирован, чтобы не возникало ошибок при одновременном доступе.

🛠 Условия задачи
✅ Основные параметры:
Параметр	Значение
Кол-во потоков	5 кассиров
Кассиры	3 на выдачу, 2 на пополнение
Баланс кассы	Общий для всех
MAX_BALANCE	1000 (нельзя превысить)
MIN_BALANCE	0 (нельзя уйти в минус)

💡 Очереди заявок
У каждого кассира есть список сумм, которые он должен обработать. Примеры:

Кассир-пополнение: [100, 200, 150]

Кассир-выдача: [50, 300, 120]

🔐 Работа с балансом
Методы deposit(int amount) и withdraw(int amount) реализуют кассу. Они:

deposit(amount) — добавляет деньги:

Если баланс + сумма > MAX_BALANCE — кассир ждёт.

Иначе — деньги добавляются, остальные кассиры уведомляются.

withdraw(amount) — снимает деньги:

Если сумма > баланс — кассир ждёт, пока деньги не поступят.

Иначе — снимает сумму и уведомляет всех.

Логика ожидания реализуется с помощью wait() и notifyAll() (или ReentrantLock и Condition — по желанию).


Пример логов
Программа выводит информацию о действиях:

Кассир 1 (выдача): выдал 100. Баланс: 400
Кассир 2 (пополнение): внес 200. Баланс: 600
Кассир 3: недостаточно средств, ожидаю...
Кассир 4: касса переполнена, ожидаю...

Также каждые 10 операций выводится:
--- Статус: баланс 750, операций: 10 ---
